<head>
  <%= stylesheet_link_tag "add" %>
</head>
<div class="jumbotron">
  <h2 class="display-4" id = "addImage">Please add an Image Link</h2>
  <div id = "form" align="center"><%= form_with scope: :images, url: images_path, local: true do |form| %>
        <% if @image.errors.any? %>
          <div id="error_explanation">
            <h2>
              <%= pluralize(@image.errors.count, "error") %> prohibited
              this article from being saved:
            </h2>
            <ul>
              <% @image.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
      <% end %>

      <p>
        <%= form.label :link %><br>
        <%= form.text_area :link %>
      </p>
      <p>
         <%= form.label :tag_list %><br>
         <%= form.text_area :tag_list %>
      </p>

      <p>
        <%= form.submit%>
      </p>
  <% end %>
  </div>

</div>


